name: Publish Package to npm

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build package
        run: |
          npx vite build
          npx tsc -p tsconfig.lib.json --skipLibCheck

      - name: Publish to npm
        run: npm publish --access public --provenance
        env:
          # Keep backward compatibility for teams still using token-based publish.
          # For npm Trusted Publishing, this can be omitted and OIDC will be used.
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
